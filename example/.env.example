# Expo public env — loaded by the example app at bundle time
EXPO_PUBLIC_APPLE_MUSIC_DEVELOPER_TOKEN=

# Generate from repo root (see docs/CLI.md):
#   yarn dev-token -- --write-env example/.env.local
